# Updates the bundled Python.xcframework and PythonHeaders from a BeeWare
# Python-Apple-support release.  Run manually and provide the release tag
# (e.g. "3.13-b13") and the new CPython package version tag (e.g. "313.11.0").

name: Update xcframework

on:
  workflow_dispatch:
    inputs:
      beeware_tag:
        description: 'BeeWare Python-Apple-support tag (e.g. 3.13-b13)'
        required: true
        type: string
      release_tag:
        description: 'CPython package version tag (e.g. 313.11.0)'
        required: true
        type: string

jobs:
  update:
    name: Update xcframework from BeeWare
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Download BeeWare tarballs
        run: |
          TAG="${{ github.event.inputs.beeware_tag }}"
          # Asset filenames use just the suffix after the dash, e.g. "3.13-b13" → "b13"
          SUFFIX="${TAG##*-}"
          BASE="https://github.com/beeware/Python-Apple-support/releases/download/$TAG"
          curl -L "$BASE/Python-3.13-macOS-support.$SUFFIX.tar.gz" -o macos.tar.gz
          curl -L "$BASE/Python-3.13-iOS-support.$SUFFIX.tar.gz"   -o ios.tar.gz

      - name: Extract tarballs
        run: |
          mkdir -p extract/macos extract/ios
          tar -xzf macos.tar.gz -C extract/macos
          tar -xzf ios.tar.gz   -C extract/ios

      - name: Rebuild multiplatform xcframework
        run: |
          # Both tarballs have Python.xcframework/ at their root
          MACOS_XC="extract/macos/Python.xcframework"
          IOS_XC="extract/ios/Python.xcframework"
          echo "macOS xcframework: $MACOS_XC"
          echo "iOS   xcframework: $IOS_XC"

          # Rebuild clean xcframework with only .framework slices (no stdlib/bin/include bloat)
          rm -rf Frameworks/Python.xcframework
          mkdir -p Frameworks/Python.xcframework

          # Copy build utilities from iOS xcframework
          cp -R "$IOS_XC/build" Frameworks/Python.xcframework/

          # iOS slices: only the Python.framework directory
          for SLICE in ios-arm64 ios-arm64_x86_64-simulator; do
            if [ -d "$IOS_XC/$SLICE/Python.framework" ]; then
              mkdir -p "Frameworks/Python.xcframework/$SLICE"
              cp -R "$IOS_XC/$SLICE/Python.framework" "Frameworks/Python.xcframework/$SLICE/"
            fi
          done

          # macOS slice: only the Python.framework
          mkdir -p Frameworks/Python.xcframework/macos-arm64_x86_64
          cp -R "$MACOS_XC/macos-arm64_x86_64/Python.framework" Frameworks/Python.xcframework/macos-arm64_x86_64/

          # Rebuild Info.plist: start from iOS xcframework's plist, add macOS entry from macOS xcframework's plist
          python3 - <<'PYEOF'
          import plistlib

          with open("extract/ios/Python.xcframework/Info.plist", "rb") as f:
              info = plistlib.load(f)
          with open("extract/macos/Python.xcframework/Info.plist", "rb") as f:
              macos_info = plistlib.load(f)

          # Find the macos-arm64_x86_64 entry from the macOS xcframework's plist
          macos_entry = next(
              e for e in macos_info["AvailableLibraries"]
              if e["LibraryIdentifier"] == "macos-arm64_x86_64"
          )
          info["AvailableLibraries"].append(macos_entry)

          with open("Frameworks/Python.xcframework/Info.plist", "wb") as f:
              plistlib.dump(info, f)
          ids = [e["LibraryIdentifier"] for e in info["AvailableLibraries"]]
          print(f"Wrote Info.plist with {len(ids)} slices: {ids}")
          PYEOF

      - name: Update PythonHeaders from macOS slice
        run: |
          MACOS_HEADERS="extract/macos/Python.xcframework/macos-arm64_x86_64/Python.framework/Versions/3.13/include/python3.13"
          echo "Copying headers from: $MACOS_HEADERS"
          rm -rf PythonHeaders
          cp -R "$MACOS_HEADERS" PythonHeaders

      - name: Commit and push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Frameworks/Python.xcframework PythonHeaders/
          git diff --cached --quiet || git commit -m "Update Python.xcframework and PythonHeaders to BeeWare ${{ github.event.inputs.beeware_tag }}"
          git tag ${{ github.event.inputs.release_tag }}
          git push origin HEAD
          git push origin ${{ github.event.inputs.release_tag }}

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create ${{ github.event.inputs.release_tag }} \
            --repo ${{ github.repository }} \
            --title "${{ github.event.inputs.release_tag }}" \
            --notes "Python 3.13 xcframework from BeeWare Python-Apple-support ${{ github.event.inputs.beeware_tag }}"

